@model WebApp.ViewModels.Admin.UserCoursesVM

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>UserCourses</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="UserCoursesVm.AppUserId" class="control-label"></label>
                <select asp-for="UserCoursesVm.AppUserId" class ="form-control" asp-items="Model.UserCoursesUserSelectList"></select>
            </div>
            <div class="form-group">
                <label asp-for="UserCoursesVm.CourseId" class="control-label"></label>
                <select asp-for="UserCoursesVm.CourseId" class ="form-control" asp-items="Model.UserCoursesCoursesSelectList"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
